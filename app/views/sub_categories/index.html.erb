

  <h1>Listing Sub Categories</h1>

<table class="tablestyling">
  <tr>
    <th>Sub Category Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sub_categories.each do |sub_category| %>
  <tr>
    <td><%= link_to "#{sub_category.name}" ,list_events_for_category_sub_category_path(sub_category) %></td>
    <td><%= link_to 'Show', sub_category %></td>
    <td><%= link_to 'Edit', edit_sub_category_path(sub_category) %></td>
    <td><%= link_to 'Delete Sub Category', sub_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
<%= paginate @sub_categories %>
<br />
</div>