<%= form_for(@sub_category) do |f| %>
  <% if @sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
      <% @sub_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="wrapper">
  <div class="field">
    <table><tr><td><%= f.label :'Provide the category name' %></td>
    <td><%= f.text_field :name %></td></tr></table>
  </div>
  <div class="actions">
    <input type="submit" value="Create" name="commit">
  </div>
</div>
<% end %>
